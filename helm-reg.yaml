
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "lms-lti-3rdpartyprovisioning-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,reg"

serviceAccountName: lms-lti-3rdpartyprovisioning-reg

cluster: es-test