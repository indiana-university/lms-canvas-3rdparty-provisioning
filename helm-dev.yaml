namespace: default
bootstrapSecret: "properties-2020-08-20"

image:
  repository: localhost:5000/lti_3rdpartyprovisioning
  pullPolicy: Never
  tag: unstable

replicaCount: 1

env:
  - name: APP_ENV
    value: dev

service:
  type: NodePort

activeProfiles: "none"